<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.github.takahirom.fontsamples"
    >

    <uses-sdk tools:overrideLibrary="com.xwray.groupie,com.xwray.groupie.kotlinandroidextensions" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PlatformFontActivity"
            android:label="@string/title_activity_platform_font"
            />
        <activity android:name=".PlatformDownloadableFontActivity" />
        <activity android:name=".SupportLibraryFontActivity" />
        <activity android:name=".SupportLibraryDownloadableFontActivity" />
        <activity android:name=".FontFetchContentProviderActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts"
            />

        <activity android:name=".FontRequestActivity" />
        <activity android:name=".FontRequestWithResourcesCompatActivity"></activity>
    </application>

</manifest>
